# 规划中模块 / Planned Module

本目录为 SmartLearn AI 的前端规划模块，当前尚未实现；生产编排已由 `docker-compose.yml` 移除。请勿在生产中依赖此目录。

---

This directory is a **planned frontend module** for SmartLearn AI and is **not yet implemented**. It has been removed from the production `docker-compose.yml` orchestration. Do **not** rely on this directory in production.
